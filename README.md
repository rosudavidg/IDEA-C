## Algoritmul de criptare IDEA

### Folosire
	Se modifica valorile `KEY` si `DATA` in fisierul `main.c`.
	Se compileaza sursa: `make build`.
	Se ruleaza executabilul: `make run`.
	Se sterge executabilul creeat: `make clean`.

	Mai usor: `make`.
